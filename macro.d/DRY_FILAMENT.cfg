[gcode_macro DRY_FILAMENT]
description: This macro contains starts heating plate to certain temp and maitain it to dry laying filament

gcode:
    M117 Starting macro: DRY_FILAMENT
    M117 Set heating bed to 50°C
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=50
    M117 Set timeout to 2 hours
    SET_IDLE_TIMEOUT TIMEOUT=7200                                                       ;
