[gcode_macro list_sensors]
gcode:
  M117 Available heaters: { action_respond_info(printer.heaters.available_heaters | join(', ')) }
  M117 Available sensors: { action_respond_info(printer.heaters.available_sensors | join(', ')) }